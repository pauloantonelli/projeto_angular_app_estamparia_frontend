import { MostarEsconderMenuMobileDirective } from './mostar-esconder-menu-mobile.directive';

describe('MostarEsconderMenuMobileDirective', () => {
  it('should create an instance', () => {
    const directive = new MostarEsconderMenuMobileDirective();
    expect(directive).toBeTruthy();
  });
});
