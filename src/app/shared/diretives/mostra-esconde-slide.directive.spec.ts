import { MostraEscondeSlideDirective } from './mostra-esconde-slide.directive';

describe('MostraEscondeSlideDirective', () => {
  it('should create an instance', () => {
    const directive = new MostraEscondeSlideDirective();
    expect(directive).toBeTruthy();
  });
});
